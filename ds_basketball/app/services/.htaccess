# Servicios de facturacion electronica SRI (firma, web service).
# Codigo de servidor: nunca se solicita por URL.
Options -Indexes
Require all denied
<IfModule mod_access_compat.c>
    Deny from all
</IfModule>
